import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const TermsInfo = styled(Box)({
  display: "flex",
  alignItems: "start",
  maxWidth: "380px",
  a: {
    margin: "0 5px",
  },
});
